<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.catface996.aiops.repository.mysql.mapper.auth.AccountMapper">

    <!-- 根据用户名查询账号 -->
    <select id="selectByUsername" resultType="com.catface996.aiops.repository.mysql.po.auth.AccountPO">
        SELECT id, username, email, password, role, status, created_at, updated_at
        FROM t_account
        WHERE username = #{username}
        LIMIT 1
    </select>

    <!-- 根据邮箱查询账号 -->
    <select id="selectByEmail" resultType="com.catface996.aiops.repository.mysql.po.auth.AccountPO">
        SELECT id, username, email, password, role, status, created_at, updated_at
        FROM t_account
        WHERE email = #{email}
        LIMIT 1
    </select>

</mapper>
